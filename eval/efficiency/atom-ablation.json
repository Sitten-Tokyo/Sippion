{
  "gate": "cargo fmt --check + cargo build --release --locked + cargo test --locked + cargo clippy --all-targets --all-features --locked -- -D warnings + scripts/retrieval-eval.py",
  "caps": {
    "10": "pass",
    "6": "pass",
    "4": "fail",
    "3": "fail"
  },
  "selected_cap": 6,
  "failure_evidence": {
    "cap_4": "packed expected-path recall 0.929 < 1.000"
  },
  "notes": "Six is the smallest tested cap that preserves the deterministic repository correctness/evidence gates. Caps four and three are disqualified regardless of token savings. End-task model validation remains required before publishing a token-reduction claim."
}
